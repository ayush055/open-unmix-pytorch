# To keep instance running when SSH disconnects

tmux new-session -s session_name
tmux list-sessions
tmux attach-session -t session_name
tmux kill-session -t session_name

Detach from any session as described above: Ctrl+B, D.

# Check when contents in a folder were modified
ls -lt

# Evaluate for single target
sudo env "PATH=$PATH" python evaluate.py --model open-unmix --targets vocals  --root ~/musdb18hq --is-wav --residual acc --outdir ~/open-unmix-pytorch/results/baseline/

# Evaluate using museval
method = museval.MethodStore()
method.load('open-unmix.pandas')
method.agg_frames_tracks_scores()

# Train
python train.py --root ~/musdb18hq --target vocals --is-wav --nb-workers 16